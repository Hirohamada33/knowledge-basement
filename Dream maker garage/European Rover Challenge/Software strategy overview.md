Computer-based autonomous navigation system.

Transfer learning is required. 

Computational intelligence-based path planning, DL in drone autonomy, DL in forestry, navigation using RL, civil infrastructure health monitoring. 

Camera: greyscale, RGB, RGB-D, thermal, infrared. 

CNN with transfer learning (addressing data scarcity, domain shifts challenge by fine-tuning pre-trained model in real time). 

Functionalities: Object detection, target recognition, terrain classification (even in previously unexplored or dynamic scenarios). 

YOLO (You only look once) has the ability to detect multiple objects in a single image. It is in conjunction with COCO (Common Objects in Context), VOC (Visual Object Classes). Evaluation metrics are Average Precision (AP) and mean Average Precision (mAP). 

Using 2D image to produce 3D imagery using post-processing elements such as photogrammetry. 
Image processing technique: Canny edge detection Hough transform
Detection of line segments (railway) -> navigation using line following

DRL with PPO. 





```c
*int function(*ptr){
	

}
int a = 5; 

function()






int swap(int& a, int& b){
	int t = b;
	b = a;
	a = t;
}

int main(){
	int n1 = 1, n2 = 2;
	swap(n1, n2);
}


int swap(int* a, int* b){
	t = *b;
	*b  = *a;
	*a = t;
}

int main(){
	int n1 = 1, n2 = 2;
	swap(&n1, &n2);
}









```